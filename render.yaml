services:
  - type: web
    name: sales-planner
    runtime: node
    region: singapore
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: GITHUB_REPO
        value: Bobo1447/sales-planner
