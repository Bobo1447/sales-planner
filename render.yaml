services:
  - type: web
    name: sales-planner
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sales-planner-db
          property: connectionString

databases:
  - name: sales-planner-db
    plan: free
